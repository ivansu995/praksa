name: Deploy App to EC2

on:
  # workflow_run:
    # workflows: ["Docker Image CI for GitHub Container registry"]
    # types:
    #   - completed
  push:
    branches:
      - master
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Add SSH key
        uses: webfactory/ssh-agent@v0.5.3
        with:
          ssh-private-key: ${{ secrets.AWS_KEY }}

            #sudo docker rmi $(sudo docker images | awk '{print $1}')
      - name: Pull docker    
        run: |
          ssh -o StrictHostKeyChecking=no ${{ secrets.AWS_USER }}@${{ secrets.AWS_HOST }} << 'EOF'
            echo ${{ secrets.GH_PERSONAL_ACCESS_TOKEN }} | sudo docker login ghcr.io -u ${{ github.actor }} --password-stdin
            cd /home/ec2-user/docker
            sudo echo "DB_PASSWORD=${{ secrets.DB_PASSWORD }}" > .env
            sudo echo "DB_USERNAME=${{ secrets.DB_USERNAME }}" >> .env
            sudo echo "DB_HOST=${{ secrets.DB_HOST }}" >> .env
            sudo echo "DB_DBNAME=${{ secrets.DB_DBNAME }}" >> .env
            sudo docker-compose down
            sudo docker pull ghcr.io/ivansu995/praksa:latest
            sudo docker pull ghcr.io/ivansu995/redis:latest
            sudo docker pull ghcr.io/ivansu995/nginx-proxy:latest
            sudo docker-compose up -d
          EOF
        # uses: appleboy/ssh-action@v0.1.2
        # with:
        #   host: ${{ secrets.AWS_HOST }}
        #   username: ${{ secrets.AWS_USER }}
        #   key: ${{ secrets.AWS_KEY }}
        #   port: 22
      
      # - name: Login to ghcr
      #   uses: docker/login-action@v3
      #   with:
      #     registry: ghcr.io
      #     username: ${{ github.actor }}
      #     password: ${{ secrets.GH_PERSONAL_ACCESS_TOKEN }}

      # - name: Stop containers and remove images
      #   run: |
      #     cd /home/ec2-user/docker
      #     sudo docker compose down
      #     sudo docker rmi ghcr.io/ivansu995/praksa:latest

      # - name: Pull Docker App image
      #   run: |
      #     sudo docker pull ghrc.io/ivansu995/praksa:latest
      #     export DB_PASSWORD=${{ secrets.DB_PASSWORD }}
      #     export DB_USERNAME=${{ secrets.DB_USERNAME }}
      #     export DB_HOST=${{ secrets.DB_HOST }}
      #     export DB_DBNAME=${{ secrets.DB_DBNAME }}
          

      # - name: Start containers
      #   run: |
      #     cd /home/ec2-user/docker
      #     sudo docker compose up -d